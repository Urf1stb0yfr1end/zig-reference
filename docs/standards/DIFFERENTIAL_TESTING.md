# Differential testing

A differential target names two independent implementations, input normalization, equivalence rules, and intentional differences. Divergence becomes a minimized regression. Comparing a function with itself is invalid evidence. The repository command reports clearly when no eligible pair is configured.
