# Release policy

Releases target Zig 0.14.0 until an accepted ADR changes the baseline. A release records source revision, textual validation evidence, known gaps, and migration notes. Tags do not raise module maturity automatically. Reproducible binary outputs may be distributed elsewhere but are never repository source.
