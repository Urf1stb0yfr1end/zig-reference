# Contributing

Query generated indexes, read `details.json`, follow dependencies, and reuse compatible modules before coding. Edit canonical sources, regenerate text, run `zig build validate-repository` with Zig 0.14.0, and disclose unavailable checks. Contributions must contain no generated binary artifacts and must follow AGENTS.md.
