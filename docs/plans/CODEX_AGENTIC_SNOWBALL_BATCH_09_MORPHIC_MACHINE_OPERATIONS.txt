CODEX AGENTIC SNOWBALL BATCH 09 — MORPHIC MACHINE OPERATIONS

MISSION

Continue the Agentic Snowball from a verified Batch 08 foundation, but first remove one repeatedly observed repository-environment ambiguity: Python-dependent validation must not succeed or fail merely because a previous shell happened to activate `.venv`.

After that environment gate is repaired and mechanically protected, begin the next system-expansion phase by giving Morphic the smallest evidence-backed target-neutral machine-operations boundary and proving that one shared core runtime can execute through the existing hosted implementation and one deterministic fake implementation without duplicating runtime semantics.

The governing ideas are:

CANONICAL COMMANDS MUST SELECT THEIR REQUIRED ENVIRONMENT.
ONE CORE RUNTIME.
MULTIPLE MACHINE IMPLEMENTATIONS.
NO SECOND SEMANTIC IMPLEMENTATION.

Batch 09 is not a universal environment manager, package manager, HAL, simulator, or RISC-V backend. It is a narrow reliability repair followed by one narrow Morphic expansion.

DO NOT:

- globally install Python packages as the repository fix;
- rely on `source .venv/bin/activate` as the permanent fix;
- hardcode `/home/...`, a username, or any machine-specific absolute interpreter path;
- vendor `jsonschema` into the repository;
- replace normal Python packaging concepts with a project-specific dependency language;
- create a universal Python launcher if a smaller conventional solution exists;
- begin Alpine;
- begin Linux boot handoff;
- implement privileged RISC-V CSR/trap/SBI behavior;
- begin UART, FDT, PLIC, CLINT, VirtIO, networking, or a hypervisor;
- create a universal HAL;
- create a new DSL, interface language, macro system, daemon, database, source scanner, or compiler substitute;
- create speculative machine operations merely because future systems may need them.

Build only what current evidence demonstrates.

STARTING STATE

Work in ~/dev/zig-reference.

Read AGENTS.md first.
Read COMMANDS.md before inventing or guessing commands.
Read docs/agentic-vocabulary.md.
Read docs/standards/SNOWBALL_YIELD.md if present.
Read docs/reports/AGENTIC_SNOWBALL_BATCH_06.md.
Read docs/reports/AGENTIC_SNOWBALL_BATCH_07.md.
Read docs/reports/AGENTIC_SNOWBALL_BATCH_07_REPAIR.md if present.
Read docs/reports/AGENTIC_SNOWBALL_BATCH_08.md.
Read the existing hosted Morphic recipe contract and source.
Read the current Agent Fast Path/preflight contracts for bounded-system-resource-plan, bounded-deterministic-event-trace, and bounded-deterministic-scheduler before opening implementation source.

Sync current main if an `origin` remote exists. If the execution environment lacks it, record that limitation and continue from the supplied checkout. Never hide environment limitations.

Record the actual base commit.
Use Zig 0.14.0 only.

The latest independently observed Batch 08 user-side state before this request was:

- 53 contracted modules;
- 53 full Agent Fast Path modules;
- 0 partial modules;
- Failure State Capsule focused regression green;
- three evidence-backed structured KNOWN diagnoses green;
- insufficient and unsupported capsules remain UNKNOWN;
- malformed capsules fail closed;
- hosted Morphic verification green at 66/66 steps and 32/32 tests;
- full repository validation green at 322/322 steps and 204/204 tests;
- working tree clean;
- benchmark-pre-morphic-52 frozen.

Do not assume those counts/results remain current. Verify actual state and record differences honestly.

IMPORTANT REPEATED ENVIRONMENT EVIDENCE

A recurring Codex log has shown:

AGENT CONTRACT ERROR: No module named 'jsonschema'
verify-hosted-morphic-runtime +- validate-agent-contracts +- run python3 failure

This has appeared even though the repository `.venv` can contain working `jsonschema` and the same validation succeeds after activating that environment.

Treat this as a repository-level execution-environment pressure case, not as proof of a specific implementation bug until traced.

The canonical dependency declaration remains tools/requirements.txt. Do not create a second dependency list.

PHASE 0 — PYTHON EXECUTION ENVIRONMENT MUST BE BORING

Before Morphic architecture work, reproduce and trace the Python interpreter selection used by Python-dependent build and canonical verification paths.

At minimum inspect:

- tools/requirements.txt;
- every build.zig path that launches Python-dependent repository validation;
- tools/developer-command.py where relevant;
- tools/query-reference.py doctor behavior;
- tools/validate-agent-contracts.py;
- tools/test-invariant-diagnosis.py;
- existing setup instructions in README/AGENTS/COMMANDS.

Answer factually in the Batch 09 report:

1. Which interpreter executes direct Python tools when `.venv` is active?
2. Which interpreter executes Python tools launched transitively by `zig build`?
3. Can those differ?
4. Which Python-dependent tools import `jsonschema`?
5. Is the recurring failure caused by ambient PATH/interpreter selection, missing setup, or something else?

Do not state the cause until demonstrated.

PHASE 0A — DEFINE ONE CANONICAL RULE

Implement the smallest conventional rule that makes repository validation predictable.

Required outcome:

- Python-dependent canonical/build-backed validation does not accidentally depend on whether an earlier shell activation happened to persist;
- the interpreter/dependency environment used by the command is deterministic enough for a cold agent to understand;
- tools/requirements.txt remains the source of Python dependency truth;
- a missing or unusable required Python environment fails early and clearly with an actionable conventional repair path;
- repository commands do not silently install dependencies;
- no global pip/uv install is required as the fix;
- no machine-specific absolute path is committed;
- normal Python, virtual-environment, PATH, subprocess, and Zig build concepts are used rather than a private environment DSL.

Prefer the smallest implementation. If build.zig can resolve one repository-approved interpreter cleanly and reuse it for Python-dependent steps, do not create a larger framework. If one tiny shared resolver is genuinely needed by multiple command surfaces, centralize it rather than duplicating selection logic.

Do not require every dependency-free Python helper to run through a heavyweight wrapper merely for symmetry.

PHASE 0B — DOCTOR MUST TELL THE TRUTH

A cold agent should not discover missing Python validation dependencies only after a deep Morphic build fails.

Strengthen the existing `agent doctor` only as much as necessary so it reports the Python validation environment truth relevant to canonical agent-contract validation.

A healthy result should make clear, in deterministic compact output, enough of the following to act correctly:

- whether the canonical validation interpreter is usable;
- whether required Python validation dependencies are importable;
- the relevant dependency declaration location;
- an exact repair/setup command or canonical setup path when unhealthy.

Do not dump `pip freeze` or environment noise.
Do not expose irrelevant host details.
Do not turn doctor into a package manager.

If a new stable ZIGREF diagnostic is justified by this repeated failure class, add one only if it maps cleanly to canonical evidence and existing diagnostic architecture. Do not contort the Batch 08 numeric invariant representation merely to force an environment error into it. An ordinary deterministic doctor failure is acceptable if that is the cleaner design.

PHASE 0C — REGRESSION MUST PROVE SHELL ACTIVATION IS NOT THE CONTRACT

Add the smallest durable regression that would fail if a future change again makes serious Python-dependent validation depend on incidental venv activation.

The regression must exercise actual repository command behavior, not merely inspect a source string.

At minimum demonstrate both of these states where the local environment permits it:

A. provisioned repository environment, ordinary activated shell -> canonical validation passes;
B. same provisioned repository environment, fresh/unactivated shell semantics -> canonical build-backed validation still selects the intended environment and passes.

If the intended final design instead deliberately requires an explicit canonical environment command rather than automatic `.venv` resolution, then state that rule clearly and make the serious canonical command invoke/require it deterministically. The user must not need to remember an undocumented prior `source` command.

Also test the negative path without destructively moving the real `.venv`: use an isolated temporary fixture, resolver seam, subprocess environment, or other safe technique. It must prove that missing/unusable Python dependencies fail with the intended clear diagnostic/repair rather than a deep unexplained `ModuleNotFoundError`.

Do not make the regression specific only to the literal string `jsonschema`; protect the interpreter/dependency-selection rule that caused the class of failure.

PHASE 0 EXIT GATE

Do not begin Morphic machine-operations edits until all of the following are true:

- root cause is recorded;
- environment-selection repair is implemented or the evidence proves no repair is necessary;
- focused regression passes;
- agent doctor is truthful;
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/validate-agent-contracts.py --self-test` passes in its intended direct environment;
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/test-invariant-diagnosis.py` passes;
- hosted Morphic canonical verification passes without relying on undocumented shell state;
- command reference remains current.

If Phase 0 cannot be made correct without a broad unrelated packaging redesign, stop and report instead of hiding the problem under global installation.

BEFORE MORPHIC EDITING — FREEZE THE REQUIREMENT MAP

After Phase 0 is green, practice the Snowball instead of assuming what needs to be built.

Run the smallest useful existing discovery surfaces, using the repaired canonical environment rules:

python3 tools/query-reference.py agent bootstrap
python3 tools/query-reference.py agent doctor
python3 tools/query-reference.py agent decide "target-neutral machine operations for the hosted Morphic runtime with deterministic fake implementation"
python3 tools/query-reference.py agent preflight run-hosted-morphic-runtime
python3 tools/query-reference.py agent preflight bounded-system-resource-plan
python3 tools/query-reference.py agent preflight bounded-deterministic-event-trace
python3 tools/query-reference.py agent preflight bounded-deterministic-scheduler
PYTHONDONTWRITEBYTECODE=1 python3 tools/test-agent-fast-path.py
PYTHONDONTWRITEBYTECODE=1 python3 tools/test-invariant-diagnosis.py
PYTHONDONTWRITEBYTECODE=1 python3 tools/check-command-reference.py --check
PYTHONDONTWRITEBYTECODE=1 python3 tools/build-agent-index.py --check
PYTHONDONTWRITEBYTECODE=1 python3 tools/validate-agent-contracts.py --self-test
PYTHONDONTWRITEBYTECODE=1 python3 tools/test-developer-minimus.py

Use agent compose/capability queries where current canonical vocabulary supports the requirement. Do not invent capability IDs merely to satisfy this request.

Before machine-operations implementation, write a frozen requirement map into docs/reports/AGENTIC_SNOWBALL_BATCH_09.md with every requirement classified as:

- existing module;
- existing recipe/composition;
- missing reusable capability;
- project-specific orchestration;
- out of scope.

Include the Python execution-environment requirement as a separate Phase 0 repair item, not as a fake Morphic capability.

Do not choose an arbitrary module quota.
Do not decide in advance that a new module must exist.
Do not recreate a mechanism already present under a different name.

BASELINE EXISTING HOSTED BEHAVIOR

Before changing Morphic runtime behavior, capture the current canonical hosted output twice and prove it is byte-identical.

Use the documented canonical command from COMMANDS.md.

Record:

- exact command;
- output byte count;
- byte equality across two identical executions;
- relevant validation command;
- exact hosted recipe identity.

The latest independently observed value was 765 bytes. Treat that as a regression clue, not as truth to force. Preserve exact output unless a demonstrated correctness repair requires a change. Explain and test any legitimate change.

ARCHITECTURAL OBJECTIVE 1 — SMALLEST MACHINE-OPERATIONS BOUNDARY

Derive the machine-specific boundary only from operations the current hosted Morphic scenario actually needs.

The boundary answers:

What must the shared core runtime ask a machine implementation to do while the rest remains target-neutral?

Prefer ordinary Zig structure and compile-time composition. Do not use runtime dynamic dispatch unless actual requirements justify it.

Potential operation classes such as output or caller-controlled logical time are examples only. Derive the real set from current code and tests.

Do not add speculative operations for interrupts, page tables, privilege changes, CSRs, SBI, MMIO, UART, FDT, disks, networking, allocation, threading, wall-clock time, or halt/shutdown unless the Batch 09 scenario actually exercises them.

The boundary must make relevant semantics explicit:

- ownership;
- borrowing;
- cleanup;
- allocation behavior;
- boundedness;
- error/failure behavior;
- determinism;
- state transitions;
- invalidation where relevant;
- environment assumptions;
- focused validation.

If no standalone reusable module is justified, keep the boundary in the smallest canonical composition location and explain why. If real pressure demonstrates a generally reusable standalone mechanism, create only the minimum module and give it the full repository contract/test/discovery treatment required by current policy.

ARCHITECTURAL OBJECTIVE 2 — DETERMINISTIC FAKE MACHINE

Add one deterministic fake implementation of exactly the same demonstrated boundary.

It exists to test target-neutral semantics without operating-system I/O or future hardware.

It must be deliberately boring:

- deterministic;
- bounded where the exercised operation is bounded;
- no network;
- no wall clock;
- no hidden global state;
- no hidden post-initialization allocation unless canonically required;
- caller-controlled or fixed state where appropriate;
- explicit failure for exhausted caller-provided capacity where applicable;
- cheap to instantiate and understand.

Do not turn it into a RISC-V simulator.
Do not model unused devices.
Do not claim cross-target equivalence before a real target exists.

ARCHITECTURAL OBJECTIVE 3 — ONE CORE RUNTIME, TWO IMPLEMENTATIONS

Refactor only as necessary so one canonical runtime path executes against:

1. the existing hosted implementation;
2. the deterministic fake implementation.

Do not copy scheduler/resource/trace orchestration.
Do not create hosted_core.zig and fake_core.zig copies.
Do not duplicate scheduling policy, resource planning, trace semantics, or output formatting.

Prefer a single generic/comptime core function instantiated with both implementations if that is the cleanest ordinary Zig 0.14.0 design.

For identical canonical input, mechanically compare every meaningful observable that can honestly be compared, including where applicable:

- normalized event trace;
- captured logical output;
- deterministic ordering;
- explicit bounded-output/capacity failure behavior.

Only claim byte equality for artifacts actually compared byte-for-byte.
Do not claim broader machine equivalence than tested.

ARCHITECTURAL OBJECTIVE 4 — AGENT-NATIVE DISCOVERY

A cold agent must be able to discover and correctly use the new boundary without broad source archaeology.

Test the cheapest sufficient path:

requirement
-> decide/query
-> selection
-> preflight
-> minimum useful contract
-> compose
-> focused validation

If discoverability is insufficient, add only the smallest canonical metadata/projection required. Do not create a second catalog, hand-author generated files, dump source into projections, or inflate every module card.

Measure exact byte sizes for representative changed agent projections and report them. Do not invent token estimates.

ARCHITECTURAL OBJECTIVE 5 — USE BATCH 08, DO NOT DECORATE WITH IT

Preflight should expose relevant correct-use obligations before integration.

If real implementation pressure produces a repeated failure class whose violated canonical invariant is established by executable evidence, add the smallest justified invariant/diagnostic/Failure State Capsule path.

Do not add a diagnostic because a compiler typo occurred once.
Do not manufacture an invariant to make Batch 08 look busy.
Do not infer causality from a native error string alone.
Known remains evidence-backed.
Unknown remains UNKNOWN.
Native Zig errors remain authoritative.

ARCHITECTURAL OBJECTIVE 6 — PREPARE, DO NOT IMPLEMENT, THE RISC-V DIFFERENCE

Leave a future RISC-V implementation with a narrow, explicit target-specific surface.

Record:

- exact machine operations a future implementation must satisfy for the tested scenario;
- semantics that remain target-neutral;
- future RISC-V concerns intentionally outside the boundary;
- next smallest evidence-backed RISC-V step.

Do not implement that RISC-V step in Batch 09.

CORRECTNESS AND STANDARD-PRACTICE DISCIPLINE

This repository deliberately tries to exploit the programming grammar a capable coding agent was already trained on. Keep ordinary concepts ordinary.

For new or changed code:

- prefer idiomatic, straightforward Zig over clever metaprogramming;
- use normal Python packaging/virtual-environment concepts for Python tooling;
- use normal JSON/schema/test/error/dependency terminology;
- keep interfaces minimal;
- make ownership and resource use explicit;
- avoid hidden allocation and ambient state;
- preserve determinism;
- keep error sets truthful;
- preserve promised failure atomicity;
- test boundaries and negative paths;
- use exact public symbols;
- keep compiler/runtime output authoritative;
- introduce project-specific vocabulary only when an ordinary established term is insufficient.

Where established programming practice and Zig already provide a good answer, require evidence before inventing a different one.

COMMAND MANUAL — MANDATORY CUMULATIVE RULE

Every repository-changing Codex run must update COMMANDS.md before completion.

This is cumulative, not batch-local.

- Add new runnable commands.
- Update changed commands, arguments, purposes, setup requirements, and canonical-vs-raw status.
- Backfill real supported commands from previous batches that remain omitted.
- Remove or accurately mark obsolete/nonexistent commands.
- Never document nonexistent commands.
- Never call a command validated unless it actually passed in the reported environment.
- Document the canonical Python validation/setup rule if Phase 0 changes it.
- Preserve Developer Minimus distinctions.

Run:

PYTHONDONTWRITEBYTECODE=1 python3 tools/check-command-reference.py --check

If a new serious aggregate verification command is genuinely justified, it must preserve ordinary output, then append exactly one LOCATIONS block followed by exactly one MINIMUS block, and preserve the underlying exit status.

Do not create an aggregate command merely for ceremony.

VOCABULARY DISCIPLINE

Use existing project terms when they actually fit, including Cost of Correct Use, Minimum Read Set, Context Efficiency, Correct-Use Preflight, Invariant-Guided Diagnosis, Failure State Capsule, Repair Locality, Snowball Yield, and Agentic Snowball.

Use ordinary terms for interpreter, virtual environment, dependency, adapter, interface, backend, fake, deterministic, target-neutral, and target-specific.

Do not rename ordinary concepts for branding.
Do not add synonyms.

Modify docs/agentic-vocabulary.md only if implementation pressure genuinely requires a new project-specific concept that ordinary terminology cannot express.

AGENTS.md DISCIPLINE

Do not add Batch 09 history to AGENTS.md.

Modify AGENTS.md only if Phase 0 or machine-operations work discovers a durable rule every future cold agent must know and the rule is not already represented by canonical commands/doctor/setup surfaces.

Prefer executable discovery over another prose instruction.

SNOWBALL YIELD — REQUIRED FACTUAL BOOKKEEPING

Create/update:

docs/reports/AGENTIC_SNOWBALL_BATCH_09.md

Record factual Snowball Yield according to docs/standards/SNOWBALL_YIELD.md.

At minimum record:

- actual base commit and Zig baseline;
- Phase 0 reproduced environment symptom;
- demonstrated root cause;
- existing mechanisms reused for environment repair;
- new environment mechanism, if any;
- whether an unactivated-shell canonical validation succeeds after repair;
- requirement map frozen before Morphic implementation;
- requirements already satisfied by modules;
- requirements already satisfied by recipes/compositions;
- missing reusable capabilities;
- project-specific orchestration;
- out-of-scope requirements;
- existing modules/recipes actually reused;
- new reusable modules/recipes created;
- known and unknown diagnostic lookups actually used;
- new evidenced diagnostics/invariants, if any;
- source reads required during authoring;
- source reads required for ordinary reuse after canonicalization;
- focused validation executed;
- aggregate validation executed;
- exact deterministic output/projection sizes measured;
- unmeasured fields;
- limits and non-claims.

Do not invent token savings, speedups, baseline costs, hidden compute, language benchmark conclusions, or agent preference rates.

VALIDATION — FOCUSED FIRST

Use actual canonical commands from COMMANDS.md after Phase 0 repairs. Do not blindly preserve a command from this request if the canonical surface legitimately changes.

Final validation must include the relevant equivalents of:

PYTHONDONTWRITEBYTECODE=1 python3 tools/test-agent-fast-path.py
PYTHONDONTWRITEBYTECODE=1 python3 tools/test-invariant-diagnosis.py
PYTHONDONTWRITEBYTECODE=1 python3 tools/build-agent-index.py --check
PYTHONDONTWRITEBYTECODE=1 python3 tools/validate-agent-contracts.py --self-test
PYTHONDONTWRITEBYTECODE=1 python3 tools/check-command-reference.py --check
PYTHONDONTWRITEBYTECODE=1 python3 tools/test-developer-minimus.py
node tools/check-port-contracts.js
node tools/test-port-public-surface.js
python3 tools/query-reference.py agent doctor

Also run:

- the new Phase 0 interpreter/dependency-selection regression;
- a canonical Python-dependent build verification from fresh/unactivated shell semantics where safely reproducible;
- focused tests for every new/changed module or recipe;
- hosted Morphic verification;
- machine-operations/fake verification if a canonical surface is created;
- hosted runtime twice for byte repeatability;
- fake runtime/scenario twice for byte repeatability;
- zig fmt --check .;
- git diff --check;
- zig build check --summary all;
- zig build test --summary all;
- zig build recipes --summary all;
- zig build conformance --summary all;
- python3 tools/developer-command.py verify-morphic-plan;
- python3 tools/developer-command.py verify-morphic-trace;
- python3 tools/developer-command.py smoke;
- python3 tools/developer-command.py validate-repository last.

Run repository property/fuzz/differential/policy gates through the canonical aggregate pipeline as defined by current repository truth. Do not claim skipped gates passed.

The final repository must demonstrate:

- Python-dependent serious validation no longer accidentally depends on prior shell activation;
- missing Python validation dependencies fail clearly and early;
- doctor exposes relevant environment health;
- Agent Fast Path remains truthful;
- Failure State Capsule regression remains green;
- hosted Morphic remains correct and deterministic;
- one shared Morphic core exists rather than copied hosted/fake logic;
- fake implementation is deterministic;
- claimed same-input hosted/fake observables compare mechanically;
- generated views are current;
- COMMANDS.md is current;
- Developer Minimus remains truthful;
- full repository validation passes;
- final worktree contains only intended task changes before completion.

INDEPENDENT-REVIEW FRIENDLINESS

A cold reviewer should be able to answer mechanically and cheaply:

1. What caused the repeated `No module named 'jsonschema'` failure?
2. Can the repaired canonical build-backed validation pass without relying on prior `source .venv/bin/activate` shell state?
3. Does the negative missing-dependency path fail with a clear repair instead of a deep ModuleNotFoundError?
4. Is there really one core Morphic runtime rather than copied hosted/fake logic?
5. Are machine operations minimal and actually exercised?
6. Does fake satisfy the same boundary?
7. Did hosted behavior remain byte-identical to baseline unless a correctness repair required otherwise?
8. Are deterministic fake runs byte-identical?
9. Are same-input logical output/trace artifacts equal where claimed?
10. Can a cold agent discover preflight/validation/environment requirements without broad source archaeology?
11. Were only evidence-backed diagnostics/invariants added?
12. Is the entire repository green and the worktree clean?

Do not require a reviewer to trust prose that can be checked mechanically.

STOP CONDITIONS

Stop and explain rather than forcing the architecture if:

- Phase 0 reveals that the recurring Python failure cannot be distinguished from an external environment limitation safely in scope;
- the environment repair requires a broad package-management system instead of a small repository rule;
- current hosted runtime cannot be separated from machine-specific operations without materially changing semantics;
- the proposed machine boundary requires speculative unused operations;
- a reusable module would duplicate an existing capability;
- fake requires a second copy of core runtime logic;
- canonical contract truth conflicts with executable source and cannot be repaired safely in scope;
- a required external tool is absent and there is no repository-approved truthful repair path.

Record limitations. Do not hide them.

COMPLETION STANDARD

Batch 09 is complete only when BOTH are true:

A. Python-dependent canonical validation has a mechanically tested, conventional, non-machine-specific environment rule that eliminates accidental reliance on prior venv activation and gives a clear negative path.

B. Morphic demonstrates the smallest useful target-neutral machine-operations boundary with one shared core and at least hosted + deterministic fake implementations, with truthful agent discovery, preflight, validation, and Snowball Yield bookkeeping.

Do not claim success because files were added.
The proof is reduced environment ambiguity, reduced runtime duplication, explicit boundaries, deterministic behavior, and mechanical validation.

Finish only when every required gate that applies has passed and the final task summary ends with exactly:

AGENTIC SNOWBALL BATCH 09 PASSED