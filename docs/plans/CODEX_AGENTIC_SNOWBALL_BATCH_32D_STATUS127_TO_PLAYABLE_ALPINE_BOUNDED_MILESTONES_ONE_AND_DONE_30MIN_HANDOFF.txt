CODEX AGENTIC SNOWBALL — BATCH 32D CONTINUATION
STATUS 127 -> INTERACTIVE SHELL -> PLAYABLE ALPINE
BOUNDED MILESTONES / ONE-AND-DONE / 30-MINUTE HANDOFF

Repository: thanks-cohn/zig-reference
Canonical path:
docs/plans/CODEX_AGENTIC_SNOWBALL_BATCH_32D_STATUS127_TO_PLAYABLE_ALPINE_BOUNDED_MILESTONES_ONE_AND_DONE_30MIN_HANDOFF.txt

Start from current main.
Inherited frontier at authoring time:
ec09015d94fbd19ecaa6737108c2b412b900d7bd

PRIMARY GOAL

Reach PLAYABLE ALPINE from the current exact Alpine v3.22.0 RV64 /bin/sh status-127 frontier.

The campaign is Alpine-first. QEMU is only the current execution vehicle. Do not spend this run on unrelated historical verifier work unless the exact current Alpine failure directly requires it.

Use the unchanged real Alpine /bin/sh as the pressure source. After every repair, retry the same shell immediately.

CURRENT PROVEN STATE

Preserve all of the following:
- exact Alpine v3.22.0 RV64 namespace identity;
- /bin/sh -> /bin/busybox from the real namespace;
- real /lib/ld-musl-riscv64.so.1 from the same namespace;
- real musl interpreter-first execution;
- PREPARE -> COMMIT -> execute;
- W+X=0;
- Batch 32D resource-owned process I/O;
- deterministic fixture/live-console backend separation;
- explicit live-console input selection independent of argv;
- live-console build surface through -Dexternal-rv64-live-console-input=true;
- no global stdin cursor;
- no BusyBox/ash/Alpine-specific behavior in neutral Morphic mechanisms.

CURRENT EXACT FAILURE

The exact real Alpine /bin/sh now reaches execution and exits status 127 after 41 traced syscalls before a persistent interactive round trip.

Observed startup pressure includes Linux/RISC-V syscall 29 ioctl with request TIOCGWINSZ (0x5413), plus other startup/metadata calls. Do not assume TIOCGWINSZ is fatal merely because it appears in the trace.

The immediate job is to identify the first causal failure on the status-127 path, implement the smallest general semantic required by the unchanged shell, and retry the same shell.

======================================================================
MILESTONE 1 — CLASSIFY THE STATUS-127 CAUSE
======================================================================

Bound this diagnostic tightly.

Add or use bounded diagnostics that answer:
- which syscall/result immediately precedes the shell's fatal path;
- which ENOSYS or other failures are tolerated;
- whether TIOCGWINSZ is causal or merely incidental;
- whether the fatal boundary is ioctl, file metadata, tty identity, process state, or another exact semantic.

Requirements:
- preserve the real shell unchanged;
- do not synthesize success;
- do not broadly implement every unsupported syscall in the trace;
- do not rewrite old verifiers unless current Alpine evidence requires it;
- prefer one precise diagnostic over broad tracing changes.

Success condition:
- one exact first causal startup semantic is identified with evidence.

Then immediately move to Milestone 2.

======================================================================
MILESTONE 2 — MINIMUM GENERAL REPAIR
======================================================================

Implement only the semantic proven causal by Milestone 1.

If TIOCGWINSZ is causal:
- implement the required Linux compatibility behavior at the Linux ABI edge;
- use authoritative Linux/RISC-V UAPI definitions;
- keep Linux ioctl request values and struct layout out of neutral Morphic resource types;
- model reusable terminal state/capabilities neutrally only if the implementation genuinely needs them;
- do not return unconditional ioctl success for unrelated requests.

If another syscall or semantic is causal, apply the same rule:
- correct layer;
- smallest reusable mechanism;
- focused test/proof;
- no application-specific shortcut.

Success condition:
- focused proof passes;
- the exact same Alpine /bin/sh is rebuilt and rerun immediately.

If the shell advances to a new failure, repeat Milestones 1 and 2 on the new first causal blocker. Do not stop merely because one syscall was repaired.

======================================================================
MILESTONE 3 — FIRST PERSISTENT INTERACTIVE ALPINE SHELL
======================================================================

Target:
- exact real Alpine /bin/sh;
- live fd0 input;
- fd1/fd2 visible console output;
- shell remains alive waiting for input;
- type: echo morphic
- observe command output: morphic
- issue at least one second command successfully afterward.

A prompt is not required to have a particular cosmetic form. The proof is real command/response behavior and continued shell life.

Do not print a fake / #, Alpine banner, or success marker from the kernel/runtime.

When this milestone is reached, record:

  ★ FIRST PERSISTENT INTERACTIVE ALPINE SHELL UNDER MORPHIC ★

Then continue immediately while time remains.

======================================================================
MILESTONE 4 — READ-ONLY PLAYABILITY
======================================================================

Run through the same live shell, in order:

  pwd
  ls /
  cat /etc/alpine-release

For each command:
- capture the first causal failure;
- implement only the missing general semantic;
- add focused proof;
- retry the same command;
- then continue to the next command.

Likely pressure areas may include cwd/path resolution, directory enumeration, stat/metadata, file reads, fd behavior, or process execution. The observed command decides priority.

Success condition:
- all three commands work through the real interactive shell;
- shell remains usable afterward.

======================================================================
MILESTONE 5 — WRITABLE /tmp + REDIRECTION
======================================================================

Run:

  cd /tmp
  echo hello > /tmp/hello
  cat /tmp/hello

Use observed pressure only.

Potential mechanisms include create/open/truncate/write, fd offsets, writable namespace state, cwd, and descriptor redirection. Implement only what the exact command proves necessary.

Success condition:
- /tmp/hello is created through the real shell;
- cat returns hello;
- shell remains usable.

======================================================================
MILESTONE 6 — PIPE
======================================================================

Run:

  echo hello | cat

Use the exact shell pressure to implement only required pipe/process/fd semantics.

Potential pressure includes pipe creation, read/write endpoints, fd inheritance, close semantics, child lifetime, wait behavior, and EOF propagation. Do not build a broad process subsystem speculatively.

Success condition:
- command outputs hello;
- shell remains usable afterward.

======================================================================
PLAYABLE ALPINE DEFINITION
======================================================================

PLAYABLE ALPINE may be claimed only when the same real interactive Alpine shell successfully completes:

  echo morphic
  <a second command>
  pwd
  ls /
  cat /etc/alpine-release
  cd /tmp
  echo hello > /tmp/hello
  cat /tmp/hello
  echo hello | cat

and remains alive for another command afterward.

When all gates pass, record:

  ★ PLAYABLE ALPINE UNDER MORPHIC ★

Do not begin apk/networking/DNS/TLS before this gate is real.

======================================================================
EXECUTION LOOP
======================================================================

For every blocker:

  exact Alpine failure
      -> classify one causal semantic
      -> implement minimum general repair
      -> focused proof
      -> retry exact same Alpine shell/command
      -> advance to next milestone

Keep iterations small and measurable.

If a diagnostic branch is not producing a concrete causal result, narrow the probe rather than expanding into unrelated subsystems.

Do not spend the run cleaning unrelated repository issues, historical verifier mismatches, or general QEMU behavior unless the current exact Alpine path depends on them.

======================================================================
ARCHITECTURAL CONSTRAINTS
======================================================================

Do not:
- special-case BusyBox, ash, /bin/sh, Alpine hashes, or expected command output in generic mechanisms;
- bypass the real Alpine namespace;
- bypass the real musl interpreter;
- add a kernel dynamic relocator;
- weaken PREPARE/COMMIT;
- weaken W+X=0;
- reintroduce a global stdin cursor;
- put Linux ioctl numbers or termios structs into neutral Morphic resources;
- implement a broad syscall/VFS/process/TTY checklist without observed pressure;
- replace deterministic regression fixtures with timing-dependent interactive tests;
- fake a prompt, Alpine banner, or success output.

======================================================================
REGRESSION FLOOR
======================================================================

Preserve:
- Batch 31G static BusyBox shell;
- Batch 32A real dynamic musl;
- Batch 32B dynamic BusyBox shell;
- Batch 32C FIRST REAL ALPINE;
- Batch 32D resource-owned process I/O and explicit live-console path;
- exact namespace identities;
- real musl interpreter-first execution;
- PREPARE/COMMIT;
- W+X=0;
- deterministic fixture semantics;
- repository contract/evidence rules.

The known historical active-Sv39 verifier/evidence-model mismatch is not the primary objective of this run. Record it if relevant; do not let it consume the campaign unless current Alpine execution depends on fixing it.

======================================================================
VALIDATION
======================================================================

Use focused tests after each mechanism repair.

Before final handoff, run the strongest applicable checks that fit the remaining handoff window, prioritizing:

  zig fmt --check build.zig projects recipes conformance
  zig build check
  zig build test-recipe-run-hosted-morphic-runtime
  PYTHONDONTWRITEBYTECODE=1 python3 tools/check-command-reference.py --check
  python3 tools/developer-command.py validate-repository

Regenerate canonical evidence/indexes when source changes require it. Do not hand-edit generated evidence when a canonical generator exists.

The live Alpine execution result is a required test for every repair in this campaign.

======================================================================
PERSISTENCE + 30-MINUTE HANDOFF
======================================================================

This run has a hard 30-minute useful-work handoff requirement.

Do not wait until the end to preserve progress.

By approximately 20–22 minutes:
- commit the strongest coherent implementation/proof achieved so far;
- push the branch;
- verify the remote commit if credentials permit;
- open or update a draft PR if the work is reviewable;
- ensure important diagnostics are in tracked report/test/code changes rather than only terminal output.

From approximately 22–25 minutes:
- continue only bounded work that can reasonably be committed before the cutoff;
- prefer one more exact Alpine retry or one focused repair over a new broad subsystem experiment.

At approximately 25 minutes:
- begin handoff preparation;
- stop initiating broad investigations;
- run the most important remaining focused checks;
- update the Batch 32D report with exact milestone status and current blocker.

At approximately 30 minutes: HARD CUTOFF
- stop starting new implementation experiments;
- commit all coherent useful work;
- push all useful commits;
- open/update the PR when possible;
- verify branch/commit/PR state;
- leave the working tree in a reviewable state;
- state exactly one next causal blocker if PLAYABLE ALPINE is incomplete.

A partial run is successful if it leaves a real reusable repair, exact Alpine evidence, and one precise next blocker. Do not sacrifice persisted work trying to force the final milestone in the last minutes.

======================================================================
REPORTING
======================================================================

Update:

  docs/reports/AGENTIC_SNOWBALL_BATCH_32D.md

Record:
- inherited main SHA;
- exact Alpine artifact/namespace identities used;
- each status-127/startup diagnostic result;
- each causal repair;
- exact same-shell retry after each repair;
- live fd0/fd1/fd2 results;
- interactive-shell result;
- every playability command result;
- PREPARE/COMMIT status;
- W+X status;
- focused tests and repository validation;
- commit/branch/PR state;
- one precise next blocker if incomplete.

Update COMMANDS.md only for durable reproduction commands.
Update README.md only if a real milestone is reached.

FINAL STATUS TABLE

  exact Alpine namespace                         PASS/FAIL
  real /bin/sh -> /bin/busybox                  PASS/FAIL
  real musl interpreter-first                   PASS/FAIL
  PREPARE/COMMIT                                PASS/FAIL
  W+X=0                                         PASS/FAIL
  status-127 cause classified                   PASS/FAIL
  first causal startup repair                   PASS/FAIL/NOT NEEDED
  live fd0 input                                PASS/FAIL
  fd1/fd2 console output                        PASS/FAIL
  persistent interactive shell                  PASS/FAIL
  echo morphic round-trip                       PASS/FAIL
  second command                                PASS/FAIL/NOT RUN
  pwd                                           PASS/FAIL/NOT RUN
  ls /                                          PASS/FAIL/NOT RUN
  cat /etc/alpine-release                       PASS/FAIL/NOT RUN
  writable /tmp                                 PASS/FAIL/NOT RUN
  redirection                                   PASS/FAIL/NOT RUN
  cat /tmp/hello                                PASS/FAIL/NOT RUN
  pipe                                          PASS/FAIL/NOT RUN
  PLAYABLE ALPINE                               PASS/FAIL
  repository validation                         PASS/FAIL/NOT RUN
  remote persistence                            PASS/BLOCKED

If PLAYABLE ALPINE is incomplete, state exactly one next causal blocker and the exact command/artifact that exposes it.