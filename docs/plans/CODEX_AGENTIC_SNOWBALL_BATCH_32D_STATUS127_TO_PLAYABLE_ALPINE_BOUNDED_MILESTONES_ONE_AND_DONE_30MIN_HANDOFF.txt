CODEX AGENTIC SNOWBALL — BATCH 32D CONTINUATION
STATUS 127 -> INTERACTIVE SHELL -> PLAYABLE ALPINE
BOUNDED LEAPS / MAXIMUM USEFUL PROGRESS / 30-MINUTE HANDOFF

Repository: thanks-cohn/zig-reference
Canonical path:
docs/plans/CODEX_AGENTIC_SNOWBALL_BATCH_32D_STATUS127_TO_PLAYABLE_ALPINE_BOUNDED_MILESTONES_ONE_AND_DONE_30MIN_HANDOFF.txt

Start from current main.
Authoring frontier:
ec09015d94fbd19ecaa6737108c2b412b900d7bd

PRIMARY GOAL

Reach PLAYABLE ALPINE as far as the 30-minute implementation window permits.

This is an Alpine-first campaign. QEMU is only the current execution vehicle. Historical verifier work, general QEMU investigation, cleanup, documentation polish, or unrelated repository repair must not consume the run unless the exact current Alpine path directly requires it.

MAXIMUM USEFUL PROGRESS RULE

Do not treat this as a one-syscall or one-fix task.

Work in bounded LEAPS. Each leap has a concrete acceptance gate. Within a leap, fix as many causally exposed blockers as are needed and reasonably achievable. After each repair, rerun the exact Alpine shell or exact failing command and keep advancing.

Do not stop after the first successful repair if the next blocker is already exposed and there is time to continue.
Do not spend excessive time perfecting one diagnostic when a bounded experiment can classify the failure.
Do not stop merely because a milestone was reached; immediately begin the next leap while time remains.

The objective is the maximum amount of real, reusable progress toward PLAYABLE ALPINE, with useful work safely persisted before the 30-minute cutoff.

CURRENT PROVEN STATE

Preserve:
- exact Alpine v3.22.0 RV64 namespace identity;
- real /bin/sh -> /bin/busybox lookup;
- real /lib/ld-musl-riscv64.so.1 from the same namespace;
- real musl interpreter-first execution;
- PREPARE -> COMMIT -> execute;
- W+X=0;
- Batch 32D resource-owned process I/O;
- deterministic fixture/live-console backend separation;
- explicit live-console input selection independent of argv;
- -Dexternal-rv64-live-console-input=true build surface;
- no global stdin cursor;
- no application-specific behavior in neutral Morphic mechanisms.

CURRENT EXACT FAILURE

The exact real Alpine /bin/sh reaches execution and exits status 127 after 41 traced syscalls before a persistent interactive round trip.

Observed startup pressure includes Linux/RISC-V syscall 29 ioctl with TIOCGWINSZ (0x5413) and other startup/metadata calls. TIOCGWINSZ is a lead, not an assumption. Identify the actual causal boundary.

======================================================================
LEAP 1 — STATUS 127 -> PERSISTENT INTERACTIVE ALPINE SHELL
======================================================================

Goal:

  real Alpine /bin/sh
      -> survives startup
      -> waits for live input
      -> accepts echo morphic
      -> prints morphic
      -> accepts another command afterward

This leap may require MULTIPLE repairs. Continue through them while they are causally exposed.

Execution loop inside this leap:
1. run the exact live-console Alpine /bin/sh;
2. capture the earliest failure that is actually causal to exit 127 or shell death;
3. distinguish tolerated failures from fatal ones;
4. implement the smallest general compatible semantic in the correct layer;
5. add a focused proof where practical;
6. rebuild and rerun the exact same shell immediately;
7. if the shell advances to another blocker, repeat without stopping.

Possible pressure includes ioctl/TTY behavior, metadata, fd semantics, process state, or another exact Linux compatibility requirement. The shell decides the order.

If TIOCGWINSZ is causal:
- implement only the required Linux ABI behavior;
- use authoritative Linux/RISC-V UAPI definitions;
- keep Linux ioctl numbers and structure layouts at the compatibility edge;
- keep neutral terminal state/capability concepts separate when genuinely reusable;
- do not return unconditional success for unrelated ioctl requests.

Acceptance gate:
- live fd0 input works;
- fd1/fd2 output is visible;
- shell stays alive;
- `echo morphic` produces `morphic`;
- a second command succeeds afterward.

When achieved, record:

  ★ FIRST PERSISTENT INTERACTIVE ALPINE SHELL UNDER MORPHIC ★

Then immediately begin Leap 2.

======================================================================
LEAP 2 — INTERACTIVE SHELL -> READ-ONLY PLAYABILITY
======================================================================

Through the SAME real shell, run in order:

  pwd
  ls /
  cat /etc/alpine-release

Do not stop after the first command succeeds.

Within this leap, implement as many causally required general mechanisms as needed to make the whole set work. Pressure may include cwd/path handling, directory enumeration, metadata/stat behavior, namespace reads, fd behavior, exec/lifetime, or related semantics.

For every blocker:
- identify the first causal missing semantic;
- implement the minimum general repair;
- prove it;
- retry the same command;
- continue through the set.

Acceptance gate:
- pwd works;
- ls / works;
- cat /etc/alpine-release works;
- shell remains usable afterward.

Then immediately begin Leap 3.

======================================================================
LEAP 3 — READ-ONLY SHELL -> WRITABLE /tmp + REDIRECTION
======================================================================

Through the same shell, run:

  cd /tmp
  echo hello > /tmp/hello
  cat /tmp/hello

Push through all causally exposed blockers needed for this bounded leap.

Likely pressure may include create/open/truncate/write, writable namespace state, fd offsets, cwd, descriptor replacement/redirection, close behavior, or related semantics. Implement only what the real command path proves necessary, but do not artificially stop after one repair.

Acceptance gate:
- cd /tmp works;
- /tmp/hello is created by the real shell;
- redirection writes hello;
- cat /tmp/hello returns hello;
- shell remains usable.

Then immediately begin Leap 4.

======================================================================
LEAP 4 — WRITABLE SHELL -> PIPES / PROCESS SEMANTICS -> PLAYABLE ALPINE
======================================================================

Run:

  echo hello | cat

Push through the bounded set of pipe/process/fd requirements directly exposed by this command.

Possible pressure includes:
- pipe creation;
- read/write endpoints;
- fd inheritance;
- close/EOF semantics;
- child execution/lifetime;
- wait/exit status;
- scheduler/process interaction needed by the exact pipeline.

Do not speculatively build a broad Unix process subsystem. But if several directly related semantics are required to complete this one pipeline, implement them in the same leap rather than stopping after each individual primitive.

Acceptance gate:
- `echo hello | cat` outputs hello;
- shell remains alive and accepts another command afterward.

When all earlier gates also remain true, record:

  ★ PLAYABLE ALPINE UNDER MORPHIC ★

======================================================================
PLAYABLE ALPINE ACCEPTANCE
======================================================================

PLAYABLE ALPINE is earned when the same real interactive Alpine shell successfully completes:

  echo morphic
  <second command>
  pwd
  ls /
  cat /etc/alpine-release
  cd /tmp
  echo hello > /tmp/hello
  cat /tmp/hello
  echo hello | cat
  <another command afterward>

and preserves:
- exact Alpine namespace;
- real BusyBox /bin/sh;
- real musl interpreter-first execution;
- PREPARE/COMMIT;
- W+X=0.

Do not begin apk/networking/DNS/TLS before PLAYABLE ALPINE is genuinely reached.

======================================================================
PACE / BOUNDED-LEAP RULES
======================================================================

The run should aggressively advance through the leap ladder.

Preferred behavior:
- diagnose only enough to identify the next causal repair;
- implement it;
- retry immediately;
- if that exposes the next blocker, continue;
- cross multiple small implementation boundaries within one leap when they belong to the same acceptance target;
- cross multiple leaps in one run if progress permits.

Avoid:
- spending most of the run on one historical verifier;
- broad QEMU investigation;
- speculative syscall checklists;
- unrelated refactors;
- documentation-first work;
- stopping after one repair when the next Alpine blocker is already visible;
- excessive test-suite runs between tiny edits when a focused test + exact Alpine retry gives faster feedback.

If a diagnosis produces no useful causal information after a bounded attempt, change the diagnostic approach rather than expanding scope indefinitely.

======================================================================
ARCHITECTURAL CONSTRAINTS
======================================================================

Do not:
- special-case BusyBox, ash, /bin/sh, Alpine hashes, or expected command output in generic mechanisms;
- bypass the real Alpine namespace;
- bypass the real musl interpreter;
- add a kernel dynamic relocator;
- weaken PREPARE/COMMIT;
- weaken W+X=0;
- reintroduce a global stdin cursor;
- place Linux ioctl numbers or termios structs in neutral Morphic resource APIs;
- fake a prompt, Alpine banner, command output, or success marker;
- replace deterministic regression fixtures with timing-dependent interactive tests.

Keep Linux ABI translation at the compatibility edge and reusable mechanisms neutral.

======================================================================
REGRESSION FLOOR
======================================================================

Preserve:
- Batch 31G static BusyBox shell;
- Batch 32A real dynamic musl;
- Batch 32B dynamic BusyBox shell;
- Batch 32C FIRST REAL ALPINE;
- Batch 32D resource-owned process I/O and explicit live-console path;
- exact namespace identities;
- real musl interpreter-first execution;
- PREPARE/COMMIT;
- W+X=0;
- deterministic fixture semantics;
- repository contract/evidence rules.

The known active-Sv39 historical verifier/evidence-model mismatch is not this run's target. Record it if relevant. Do not spend the run fixing it unless the exact current Alpine path depends on it.

======================================================================
VALIDATION STRATEGY
======================================================================

During active implementation:
- prefer focused tests for the mechanism just changed;
- always rerun the exact Alpine shell/command that exposed the blocker;
- do not burn large portions of the implementation window repeatedly running the full repository suite after every small repair.

When a coherent leap or major repair is reached, run the relevant stronger checks.

Before final handoff, run the strongest applicable checks that fit the remaining handoff window, prioritizing:

  zig fmt --check build.zig projects recipes conformance
  zig build check
  zig build test-recipe-run-hosted-morphic-runtime
  PYTHONDONTWRITEBYTECODE=1 python3 tools/check-command-reference.py --check
  python3 tools/developer-command.py validate-repository

Regenerate canonical evidence/indexes when source changes require it. Do not hand-edit generated evidence when a canonical generator exists.

======================================================================
PERSISTENCE / MAXIMUM PROGRESS / 30-MINUTE HANDOFF
======================================================================

The 30-minute cutoff protects progress. It is NOT a reason to narrow the implementation ambition during the first part of the run.

0–20 MINUTES — PUSH
- work directly on the leap ladder;
- cross as many causally related blockers as practical;
- keep exact Alpine retries frequent;
- make coherent commits whenever a meaningful reusable repair or leap is reached;
- do not stop merely to prepare a final report.

BY APPROXIMATELY 20–22 MINUTES — FIRST PERSISTENCE CHECKPOINT
- commit the strongest coherent progress reached so far;
- push the branch;
- verify the remote commit if credentials permit;
- open or update a draft PR if reviewable;
- ensure important diagnostics and proofs are tracked, not only in terminal scrollback.

22–25 MINUTES — KEEP ADVANCING, BUT BOUNDED
- continue toward the next leap if the next work is reasonably committable;
- prefer another exact Alpine repair/retry over broad cleanup;
- if a new subsystem would clearly exceed the remaining window, leave it as the next causal blocker rather than starting an unbounded implementation.

AROUND 25 MINUTES — BEGIN HANDOFF PREPARATION
- preserve current implementation;
- run the most important focused/aggregate checks that fit;
- update the Batch 32D report with exact achieved leap(s), failed gate, tests, and next blocker;
- commit/push again if new useful work landed after the first checkpoint.

AT APPROXIMATELY 30 MINUTES — HARD CUTOFF
- stop starting new implementation experiments;
- commit every coherent useful change;
- push all useful commits;
- open/update the PR when possible;
- verify branch/commit/PR state;
- leave a reviewable working state;
- if PLAYABLE ALPINE is incomplete, state exactly ONE next causal blocker and the exact shell/command that exposes it.

A run that reaches only part of a leap is still useful if the implementation is real, persisted, and leaves an exact next blocker. But during the active window, optimize for maximum real progress rather than deliberately choosing tiny slices.

======================================================================
REPORTING
======================================================================

Update:

  docs/reports/AGENTIC_SNOWBALL_BATCH_32D.md

Record:
- inherited main SHA;
- exact Alpine artifact/namespace identities;
- each major causal blocker encountered;
- repairs implemented;
- exact Alpine retry after repairs;
- highest completed leap;
- partial progress inside the current leap;
- live fd0/fd1/fd2 result;
- interactive-shell result;
- each playability command result;
- PREPARE/COMMIT and W+X status;
- focused tests and repository validation;
- commit/branch/PR state;
- one exact next blocker if incomplete.

Update COMMANDS.md only for durable reproduction commands.
Update README.md only when a real milestone is reached.

FINAL STATUS TABLE

  exact Alpine namespace                         PASS/FAIL
  real /bin/sh -> /bin/busybox                  PASS/FAIL
  real musl interpreter-first                   PASS/FAIL
  PREPARE/COMMIT                                PASS/FAIL
  W+X=0                                         PASS/FAIL

  LEAP 1: status127 -> interactive shell         PASS/PARTIAL/FAIL
  live fd0 input                                PASS/FAIL/NOT RUN
  fd1/fd2 console output                        PASS/FAIL/NOT RUN
  echo morphic                                  PASS/FAIL/NOT RUN
  second command                                PASS/FAIL/NOT RUN

  LEAP 2: read-only playability                  PASS/PARTIAL/FAIL/NOT RUN
  pwd                                           PASS/FAIL/NOT RUN
  ls /                                          PASS/FAIL/NOT RUN
  cat /etc/alpine-release                       PASS/FAIL/NOT RUN

  LEAP 3: writable /tmp + redirection            PASS/PARTIAL/FAIL/NOT RUN
  cd /tmp                                       PASS/FAIL/NOT RUN
  echo hello > /tmp/hello                       PASS/FAIL/NOT RUN
  cat /tmp/hello                                PASS/FAIL/NOT RUN

  LEAP 4: pipe/process semantics                 PASS/PARTIAL/FAIL/NOT RUN
  echo hello | cat                              PASS/FAIL/NOT RUN

  PLAYABLE ALPINE                               PASS/FAIL
  repository validation                         PASS/FAIL/NOT RUN
  remote persistence                            PASS/BLOCKED

If incomplete, state the highest completed leap, the partial progress in the current leap, and exactly one next causal blocker.