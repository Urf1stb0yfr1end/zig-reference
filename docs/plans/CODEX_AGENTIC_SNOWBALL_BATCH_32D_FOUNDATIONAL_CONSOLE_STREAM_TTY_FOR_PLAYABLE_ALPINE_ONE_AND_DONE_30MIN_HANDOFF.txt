CODEX AGENTIC SNOWBALL — BATCH 32D
CONSOLE / PROCESS I/O FOUNDATION FOR PLAYABLE ALPINE

Repository: thanks-cohn/zig-reference
Canonical path:
docs/plans/CODEX_AGENTIC_SNOWBALL_BATCH_32D_FOUNDATIONAL_CONSOLE_STREAM_TTY_FOR_PLAYABLE_ALPINE_ONE_AND_DONE_30MIN_HANDOFF.txt

Inherited main frontier:
7e9d55db4d30312d11d0e21e4943237adc4481ff

GOAL

Advance the existing exact Alpine v3.22.0 RV64 /bin/sh execution toward a real interactive and then playable Alpine environment.

Use the existing Alpine namespace and unchanged /bin/sh as the pressure source. Implement reusable mechanisms required by observed failures. Keep Linux-specific ABI details at the Linux compatibility edge.

CURRENT VERIFIED STATE

Batch 32C already proves:
- exact Alpine v3.22.0 RV64 minirootfs identity;
- complete serialized namespace transport;
- runtime /bin/sh -> /bin/busybox lookup;
- PT_INTERP=/lib/ld-musl-riscv64.so.1 resolved from the same namespace backing;
- real musl interpreter enters U-mode first;
- PREPARE -> COMMIT -> execute;
- exact /bin/sh -c 'echo alpine' output alpine\n;
- status 0;
- W+X=0;
- no kernel dynamic relocator or direct-main bypass.

Preserve these properties.

CURRENT BLOCKER

A real /bin/sh invocation without -c reaches execution but does not become interactive.

The external-process stdin implementation is still backed by the deterministic fixture:

  const syscall_stdin = "stdin-25b-proof";

Linux read(fd=0, ...) consumes that fixed buffer and then receives EOF. The previous no--c run therefore did not consume live QEMU keyboard input.

TASK 1 — PROCESS I/O RESOURCE

Create or extend the existing Morphic resource/operation model so process byte I/O supports backend-specific state.

Requirements:
- readable byte-source and writable byte-sink semantics;
- bounded transfers through the existing checked user-memory copy path;
- capability enforcement;
- state owned by the resource/backend instance, not one global stdin cursor;
- deterministic fixture backend preserved for existing tests;
- live QEMU console backend for external interactive processes;
- Linux syscall numbers, errno values, ioctl numbers, termios layouts, BusyBox names, Alpine paths, and RISC-V register conventions must remain outside the neutral resource abstraction.

Do not create a parallel I/O architecture if the existing Morphic operation/resource model can be extended cleanly.

TASK 2 — LIVE QEMU INPUT

Inspect the existing freestanding platform console implementation and connect the appropriate QEMU RISC-V input mechanism to the process-I/O resource.

Requirements:
- human keyboard input reaches the resource bound to fd 0;
- process output on fd 1/fd 2 reaches the machine console;
- deterministic regression fixtures remain independent from live console state;
- platform-specific QEMU/UART/SBI details stay in the platform backend;
- no Alpine-specific input path.

Add focused tests for the resource behavior and, where practical, a small generic userspace read/write proof under QEMU.

TASK 3 — RETRY EXACT ALPINE /bin/sh

After live input works, immediately retry the existing exact Alpine namespace with:

  /bin/sh

Do not use -c.
Do not substitute another shell.
Do not inject a scripted success prompt or Alpine banner.

For each failure:
1. capture the first causal failure;
2. identify the required semantic;
3. place it in the correct layer: Morphic core/resource, platform backend, or Linux compatibility edge;
4. implement the smallest reusable mechanism that satisfies the observed requirement;
5. add focused proof;
6. retry the same /bin/sh.

Continue through additional blockers while they are directly exposed by the unchanged shell.

TTY / IOCTL

If /bin/sh requires terminal ioctls or termios behavior:
- identify the exact observed Linux request and arguments;
- implement the required semantic only;
- keep Linux ioctl numbers and struct encodings in the Linux compatibility layer;
- keep neutral terminal state/capabilities outside the Linux-specific layer when such state is genuinely reusable;
- do not return unconditional success for unsupported ioctls.

OTHER SYSCALLS

Unsupported syscalls seen in traces are not automatically required.

Only implement a syscall-related semantic when the unchanged /bin/sh demonstrates that it is a causal blocker. Use authoritative Linux/RISC-V UAPI definitions for ABI translation.

INTERACTIVE SHELL ACCEPTANCE

A first interactive-shell milestone requires all of the following:
- exact Alpine namespace remains verified;
- /bin/sh resolves to the real namespace BusyBox;
- real musl interpreter executes first;
- PREPARE -> COMMIT -> execute remains intact;
- W+X=0;
- live QEMU keyboard input reaches fd 0;
- fd 1/fd 2 output reaches the console;
- shell remains alive waiting for input;
- a human can type: echo morphic
- visible output is: morphic
- shell accepts another command afterward.

Do not claim PLAYABLE ALPINE from only this result.

PLAYABLE ALPINE PRESSURE

Once the interactive shell is stable, run these commands in order:

  pwd
  ls /
  cat /etc/alpine-release
  cd /tmp
  echo hello > /tmp/hello
  cat /tmp/hello
  echo hello | cat

Use the same failure -> semantic -> implementation -> proof -> retry loop.

These commands should drive only the mechanisms actually needed for:
- current working directory/path handling;
- directory enumeration and metadata;
- namespace file reads;
- writable filesystem state;
- process exec/lifetime;
- fd inheritance and redirection;
- pipes.

If all commands work and the interactive shell remains usable, document PLAYABLE ALPINE as reached.

Do not begin apk/networking/TLS until playable Alpine is established.

ARCHITECTURAL CONSTRAINTS

Do not:
- special-case BusyBox, ash, /bin/sh, expected output, artifact hashes, or Alpine paths in generic kernel/resource mechanisms;
- move the dynamic linker into the kernel;
- add kernel-side dynamic relocations;
- bypass PREPARE/COMMIT;
- weaken W+X policy;
- replace deterministic tests with timing-dependent keyboard tests;
- share fixture input cursor/state with live-console resources;
- place Linux ioctl/termios encodings in neutral Morphic types;
- implement a broad syscall/VFS/process/TTY checklist without observed pressure;
- print ALPINE or / # from the kernel as proof.

REGRESSION FLOOR

Preserve:
- Batch 31G static BusyBox shell proof;
- Batch 32A real dynamic musl proof;
- Batch 32B dynamic BusyBox shell proof;
- Batch 32C real Alpine proof;
- exact Alpine namespace identities;
- deterministic syscall-fixture tests;
- W+X=0;
- repository validation/evidence rules.

VALIDATION

Use focused tests during implementation. After coherent shared-runtime changes, run the relevant existing verifiers.

Before final handoff, run the strongest applicable repository checks, including:

  zig fmt --check build.zig projects recipes conformance
  zig build check
  zig build test-recipe-run-hosted-morphic-runtime
  PYTHONDONTWRITEBYTECODE=1 python3 tools/check-command-reference.py --check
  python3 tools/developer-command.py validate-repository

Regenerate canonical validation evidence when required by source-digest changes. Do not edit generated evidence manually.

PERSISTENCE

Commit and push coherent working progress. Open a draft PR when the branch contains reviewable progress. Do not wait for the final playable-Alpine milestone before preserving a valid reusable mechanism.

REPORT

Create/update:

  docs/reports/AGENTIC_SNOWBALL_BATCH_32D.md

Record:
- inherited main SHA;
- implementation changes;
- focused tests;
- exact Alpine retries;
- each causal blocker and repair;
- interactive-shell result;
- playability command results;
- PREPARE/COMMIT and W+X status;
- regressions/validation;
- commit/branch/PR state;
- next causal blocker if incomplete.

Update COMMANDS.md for durable commands only.
Update README.md only when a real milestone is reached.

FINAL STATUS TABLE

  Batch 32C real Alpine regression             PASS/FAIL
  exact namespace identity                     PASS/FAIL
  process I/O resource                         PASS/FAIL
  fixture/live backend separation              PASS/FAIL
  deterministic fixture regression             PASS/FAIL
  live QEMU console input                      PASS/FAIL
  fd0 live input                               PASS/FAIL
  fd1/fd2 console output                       PASS/FAIL
  real Alpine /bin/sh entered                  PASS/FAIL
  real musl interpreter-first                  PASS/FAIL
  PREPARE/COMMIT                               PASS/FAIL
  W+X=0                                        PASS/FAIL
  persistent interactive shell                 PASS/FAIL
  echo morphic round-trip                      PASS/FAIL
  pwd                                           PASS/FAIL/NOT RUN
  ls /                                          PASS/FAIL/NOT RUN
  cat /etc/alpine-release                       PASS/FAIL/NOT RUN
  writable /tmp                                 PASS/FAIL/NOT RUN
  redirection                                   PASS/FAIL/NOT RUN
  pipe                                          PASS/FAIL/NOT RUN
  repository validation                        PASS/FAIL/NOT RUN
  remote persistence                           PASS/BLOCKED

If incomplete, state the next causal blocker precisely.
