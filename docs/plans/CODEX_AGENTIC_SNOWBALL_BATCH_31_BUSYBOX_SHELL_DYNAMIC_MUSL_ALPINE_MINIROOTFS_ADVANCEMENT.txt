CODEX AGENTIC SNOWBALL — BATCH 31
EXACT STATIC BUSYBOX SHELL COMPLETION

Repository:
thanks-cohn/zig-reference

Canonical request path:
docs/plans/CODEX_AGENTIC_SNOWBALL_BATCH_31_BUSYBOX_SHELL_DYNAMIC_MUSL_ALPINE_MINIROOTFS_ADVANCEMENT.txt

NOTE: the filename is retained for continuity, but the scope is intentionally
narrowed. Batch 31 is ONLY the exact static BusyBox shell campaign. Dynamic musl,
dynamic BusyBox, and Alpine minirootfs are deferred to fresh later runs.

STARTING TRUTH

- Main is green after Batch 30 recovery.
- Exact pinned static musl is already proven under Morphic.
- Do not redo formatting recovery or static-musl architecture.
- Do not begin with the full repository validation suite.
- Use focused tests while iterating, then full validation near the end.

EXACT PRESSURE ARTIFACT

Alpine v3.22 package:
    busybox-static-1.37.0-r20.apk

Package SHA-256:
    29072f0a72ff8f2ff19a8be703cf0e68d4722ea4dc0acf4830acc932f98cf31c

Executable:
    bin/busybox.static

Executable SHA-256:
    62831fb7c4a0da509481107a8aeb022244235c5dced18101e3d39131d303d704

Measured prepared image:
    245 pages

Use the SAME exact executable bytes for the golden reference and Morphic proof.
Do not patch, relink, truncate, vendor, or substitute the artifact.

FIRST OBSERVED BLOCKER

The exact BusyBox image already passes ELF planning. Its first current failure is
PREPARE-time capacity: the machine proof still carries a historical tiny
prepared-image capacity around 4 pages while BusyBox materializes to 245 pages.
The failure occurs before destructive COMMIT.

Repair this with the smallest correct GENERAL BOUNDED machine-adapter policy.

245 is measured pressure, NOT a permanent Morphic architectural constant.

Requirements:

- reuse the existing ExecPlan -> MaterializedImage -> PREPARE/COMMIT -> Sv39 -> U-mode path;
- do not create a BusyBox-specific loader;
- keep prepared image capacity as explicit replaceable proof/machine policy;
- reserve required image backing before COMMIT;
- reserve required Sv39 table backing before COMMIT;
- perform no ordinary backing allocation after destructive COMMIT begins;
- preserve exact final page permissions;
- preserve W+X=0;
- preserve the existing static-musl result;
- add a focused insufficient-capacity proof showing failure occurs before COMMIT and leaves the live image intact.

Then retry the SAME BusyBox immediately. Do not stop to write a report or run the
entire repository suite after merely changing the capacity policy.

MANDATORY COMMAND LADDER

1. busybox.static true
   Required: status 0

2. busybox.static echo batch31
   Required: exact output `batch31` and status 0

3. busybox.static sh -c 'echo batch31'
   Required: exact output `batch31` and status 0

Step 3 is the Batch 31 finish line.

For every repairable runtime failure in this ladder:

    observe first real failure
        -> classify reusable Morphic mechanism vs Linux compatibility-edge behavior
        -> implement the smallest correct repair
        -> add focused causal proof
        -> retry the SAME exact command immediately

Do not prebuild a guessed BusyBox syscall checklist. Let the exact artifact expose
what is actually missing. Keep Linux-specific policy at the Linux personality
edge and keep neutral reusable mechanisms in Morphic.

DO NOT STOP AT INTERMEDIATE CHECKPOINTS

The following are NOT Batch 31 success:

- capacity increased but BusyBox still does not run;
- BusyBox reaches U-mode;
- the first new runtime call is observed;
- `true` passes but `echo` does not;
- `echo` passes but `sh -c` does not;
- a report says more work is needed.

If a failure is reasonably repairable in the run, repair it and continue.
Only stop before shell success for a genuinely nontrivial blocker, with exact
observed evidence and a clear explanation of why it could not reasonably be
completed in the remaining run.

SHELL SUCCESS MUST BE REAL

The exact invocation must genuinely be:

    busybox.static sh -c 'echo batch31'

BusyBox must receive the intended argv and execute its shell path. Do not replace
the shell command with a direct echo invocation or synthetic fixture and claim
shell success.

ARCHITECTURAL RULES

Minimum permanent mechanism. Maximum inherited civilization.

Morphic remains the neutral foundation. QuirkM native userspace, Linux
ABI/personality userspace, and WebAssembly/WASI userspace are parallel software
worlds above it. No one software civilization owns the center.

Pressure discovers missing capabilities; it does not define permanent API shape.
Compatibility belongs at replaceable edges. Reusable generative mechanisms belong
at the center.

VALIDATION

During iteration use focused tests and keep retry distance short.

After the exact shell command succeeds, run the complete canonical repository
validation surface, including formatting, build/check, repository validation,
index/dependency checks, command-reference and repository-policy checks, inherited
Batch 26 machine proof, inherited exact static-musl proof, and the new exact
static-BusyBox proof.

Regenerate any canonical generated evidence using repository tooling rather than
editing generated truth by hand.

REPORT

Only after implementation, create/update:

    docs/reports/AGENTIC_SNOWBALL_BATCH_31.md

Record the exact artifact identities, 245-page pressure, capacity/backing repair,
real runtime failures encountered in order, repairs, exact true/echo/sh results,
proofs, and validation result.

EXPLICIT NON-GOALS FOR THIS RUN

Do not begin dynamic musl, dynamic BusyBox, Alpine minirootfs, apk, QEMU, KVM,
Wayland/XFCE, or WebAssembly/WASI implementation in Batch 31.

Expected next fresh campaigns after this one:

    Batch 32: real dynamic musl -> dynamic BusyBox shell
    Batch 33: real Alpine minirootfs -> /bin/sh
    later: Alpine usability -> apk -> QEMU pressure

SUCCESS DEFINITION

Batch 31 is complete only when the exact pinned busybox.static SHA-256
62831fb7c4a0da509481107a8aeb022244235c5dced18101e3d39131d303d704
passes all three under Morphic:

    busybox.static true
    busybox.static echo batch31
    busybox.static sh -c 'echo batch31'

with expected status/output while preserving exact artifact identity,
PREPARE/COMMIT atomicity, W+X=0, inherited static-musl success, and green final
repository validation.
