{
  "name": "run-hosted-morphic-runtime",
  "problem_solved": "execute one bounded deterministic target-neutral Morphic core through hosted, fake, and freestanding RISC-V execution boundaries",
  "status": "implemented-unverified",
  "selected_modules": [
    "bounded-system-resource-plan",
    "bounded-deterministic-scheduler",
    "bounded-deterministic-event-trace",
    "physical-memory-region-set",
    "physical-page-frame-allocator",
    "distinct-memory-address-types",
    "physical-page-frame-number-and-address-conversion"
  ],
  "selection_reasons": [
    "The resource planner derives fixed storage and initialization order.",
    "The scheduler owns target-neutral time and runnable ordering policy.",
    "The event trace records normalized bounded behavior.",
    "The existing physical-region and page-frame modules turn only the eight-page linker-reserved interval into deterministic owned frames; address types and PFN conversion preserve domain and alignment truth."
  ],
  "direct_dependencies": [
    "bounded-system-resource-plan",
    "bounded-deterministic-scheduler",
    "bounded-deterministic-event-trace",
    "physical-memory-region-set",
    "physical-page-frame-allocator",
    "distinct-memory-address-types",
    "physical-page-frame-number-and-address-conversion"
  ],
  "dependency_order": [
    "fixed-capacity-vector",
    "fixed-capacity-priority-queue",
    "fixed-capacity-topological-sort",
    "fixed-bump-allocator",
    "bounded-system-resource-plan",
    "bounded-deterministic-scheduler",
    "bounded-deterministic-event-trace",
    "bit-set",
    "bitmap-allocator",
    "checked-half-open-range",
    "distinct-memory-address-types",
    "validated-enum-decoder",
    "physical-page-frame-number-and-address-conversion",
    "physical-memory-region-set",
    "physical-page-frame-allocator"
  ],
  "public_endpoints_used": [
    "Planner.plan",
    "BoundedDeterministicScheduler",
    "schedule",
    "advanceTo",
    "nextReady",
    "BoundedDeterministicEventTrace",
    "append",
    "render",
    "runCore",
    "HostedMachine.writeTask",
    "FakeMachine.writeTask",
    "PhysicalAddress.init",
    "PhysicalMemoryRegionSet.add",
    "PhysicalPageFrameAllocator.initFromRegions",
    "PhysicalPageFrameAllocator.allocate",
    "PhysicalPageFrameAllocator.release",
    "PhysicalPageFrameAllocator.freeCount",
    "PhysicalPageFrameAllocator.allocatedCount",
    "PhysicalPageFrameNumber.toAddress"
  ],
  "adapters_required": [
    "recipes/run-hosted-morphic-runtime/src/recipe.zig",
    "recipes/run-hosted-morphic-runtime/src/freestanding_riscv64.zig"
  ],
  "inherited_guarantees": [
    "Fixed resource derivation, stable runnable ordering, caller-controlled time, bounded trace storage, and explicit capacity failures.",
    "One shared core owns planning, scheduling, tracing, and output formatting; hosted and fake machines provide bounded task output, while the freestanding RISC-V adapter adds entry, stack, transport, shutdown, and panic behavior plus one bounded resumable S-mode breakpoint trap, one bounded asynchronous supervisor timer interrupt, and a separate four-tick bounded monotonic supervisor timer phase around the same shared execution path. A small freestanding-only adapter consumes the four recorded rdtime observations through BoundedDeterministicScheduler.advanceTo after all corresponding sret returns and records bounded readiness/order evidence.",
    "The synchronous breakpoint alone advances sepc by its ELF-proven 32-bit instruction width. Timer interrupts leave sepc unchanged. The one-shot masks STIE and sets the deadline to maximum; each non-final repeated tick re-arms from newly observed time plus 100000 counter units, and the fourth tick masks STIE and sets the deadline to maximum before sret.",
    "Scheduler-facing machine time uses the raw RV64 rdtime value as u64 through an identity mapping, so the bridge performs no narrowing or arithmetic; advanceTo retains the scheduler time-reversal rejection and ordering semantics.",
    "The freestanding adapter reserves one eight-page, 4096-byte-aligned NOLOAD pool in its ELF, reads the linker bounds and actual satp CSR, registers exactly that interval as usable, and exercises the existing allocator. Direct access is valid only for the mechanically observed Bare mode; this is neither platform RAM discovery nor a production PMM, and Sv39 remains inactive."
  ],
  "gaps_discovered": [
    "The hosted path still relies on Linux userspace. The freestanding riscv64 path proves one fixed-frame resumable synchronous breakpoint trap, one one-shot supervisor timer interrupt, one finite four-delivery monotonic repeated-tick phase, and non-preemptive scheduler decisions driven by those observations after sret; indefinite periodic ticking, timekeeping accuracy, preemption, sleeping tasks, clock accuracy, nested traps/interrupts, multiple harts, floating-point/vector context, virtual-memory activation, hardware discovery, and Linux/POSIX/Alpine compatibility remain out of scope or unimplemented.",
    "Physical-memory ownership is limited to the linker-reserved eight-page pool. No DTB/RAM discovery, reclamation, general allocator policy, active Sv39, or virtual mapping is claimed."
  ],
  "future_systems_enabled": [
    "hosted runtime experiments",
    "freestanding riscv64 runtime experiments",
    "later bounded scheduler-facing time-source and machine-boundary work without preemption",
    "later non-preemptive timeout orchestration using an explicitly declared counter-unit policy",
    "later active-Sv39 work using explicitly owned frames rather than guessed platform RAM"
  ],
  "intended_build_commands": [
    "zig build run-hosted-morphic-runtime",
    "zig build run-fake-morphic-runtime",
    "zig build test-recipe-run-hosted-morphic-runtime",
    "zig build verify-hosted-morphic-runtime",
    "zig build install-riscv64-morphic-runtime -Dtarget=riscv64-linux-musl --prefix PATH",
    "python3 tools/verify-riscv64-morphic-runtime.py",
    "zig build install-freestanding-riscv64-morphic-runtime --prefix PATH",
    "python3 tools/verify-freestanding-riscv64-morphic-runtime.py",
    "python3 tools/verify-freestanding-riscv64-supervisor-trap.py",
    "python3 tools/verify-freestanding-riscv64-supervisor-timer.py",
    "python3 tools/verify-freestanding-riscv64-supervisor-ticks.py",
    "python3 tools/verify-freestanding-riscv64-scheduler-time.py",
    "python3 tools/verify-freestanding-riscv64-physical-memory.py"
  ],
  "intended_smoke_commands": [],
  "validation_evidence_references": []
}
