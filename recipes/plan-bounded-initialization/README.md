# Plan bounded initialization

This recipe allocates intrusive nodes from caller-owned bump storage, gives work deterministic reusable slot identities, computes a dependency order, and coordinates the ready order through the priority queue. It proves real ownership and state transfer across all five new foundations.

Run `zig build test-recipe-plan-bounded-initialization` independently or `zig build recipes` with the aggregate.
