# Mastery

This boundary describes semantic intent rather than an ABI. Linux personalities translate registers and errno only at their edge; Native or Wasm callers can construct the same requests. In C a tagged union and switch are attractive, but tag/payload agreement remains conventional. Zig's tagged union makes variants exhaustive. Zig cannot choose resource or atomic-output policy for the backend.
