# Bounded address space and exec image

Linux-independent bounded mapping state plus failure-atomic main/interpreter ELF planning. It composes project 54 and enforces W+X=0; it does not implement Linux syscalls or userspace relocations.

See [port.js](port.js) for Zig-version migration constraints.
