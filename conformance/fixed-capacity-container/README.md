# fixed-capacity-container conformance foundation

The aggregate conformance command executes the real behavioral unit contracts for `fixed-capacity-vector`. It does not use an `expect(true)` harness and does not award conformance maturity. A dedicated shared adapter remains required before other implementations can claim this suite.
